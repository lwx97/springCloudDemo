server:
  port: 9999
spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lwx97/springCloudDemo.git
          search-paths: mySpringCloudConfig
          # 公共仓库，可不需要输入账户密码
          #username:
          #password:
          default-label: master